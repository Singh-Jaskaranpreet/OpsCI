workflow:
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
    - if: '$CI_COMMIT_BRANCH == "feature/front-back"'
    - when: never

image: node:18

stages:
  - test
  - unit-test
  - build-docker
  - deploy

# 1. STAGE TEST : Vérification avec syntaxe "Block Scalar" (|) pour éviter les erreurs
check-files:
  stage: test
  script:
    - echo "Vérification de l'arborescence complète..."
    - ls -R
    - |
      echo "Vérification du Frontend..."
      if [ ! -f frontend/index.html ] || [ ! -f frontend/script.js ] || [ ! -f frontend/Dockerfile ] || [ ! -f frontend/nginx.conf ]; then 
        echo "Erreur: Fichiers Frontend critiques manquants"; exit 1; 
      fi
    - |
      echo "Vérification des Backends..."
      if [ ! -f backend/main.py ] || [ ! -f backend/Dockerfile ] || [ ! -f recommendation/recom.py ] || [ ! -f recommendation/Dockerfile ]; then 
        echo "Erreur: Fichiers Backend ou Recommendation critiques manquants"; exit 1; 
      fi

# 2. STAGE UNIT-TEST
frontend-jest:
  stage: unit-test
  script:
    - echo "Installation des dépendances de test à la racine..."
    - npm install jest jest-environment-jsdom jest-junit
    
    - echo "Lancement des tests sur __tests__/unit_test.js..."
    # On reste à la racine pour que Jest voie à la fois le dossier __tests__ et le dossier frontend
    - npx jest __tests__/unit_test.js --ci --reporters=default --reporters=jest-junit --outputFile=frontend/junit.xml
  artifacts:
    when: always
    reports:
      junit: frontend/junit.xml
  rules:
    - exists:
        - __tests__/unit_test.js

backend-syntax:
  stage: unit-test
  image: python:3.11-slim
  script:
    - pip install -r backend/requirements.txt
    - pip install -r recommendation/requirements.txt
    - python -m py_compile backend/*.py
    - python -m py_compile recommendation/*.py

# 3. STAGE BUILD-DOCKER
build-microservices:
  stage: build-docker
  image: docker:latest
  services:
    - docker:dind
  variables:
    DOCKER_DRIVER: overlay2
  script:
    - docker compose build

# 4. STAGE DEPLOY
pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp frontend/index.html frontend/style.css frontend/script.js frontend/login.html frontend/register.html public/
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'